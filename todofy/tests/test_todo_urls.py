# from django.urls import reverse


# def test_todo_url(client, live_server):
#     response = client.get(live_server.url + reverse('todo_index'))

#     assert response.status_code == 200


# def test_todo_history_url(client, live_server):
#     response = client.get(live_server.url + reverse('todo_history'))

#     assert response.status_code == 200
